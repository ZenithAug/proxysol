# Plan: Add Telegram and X Logos to FinalCTA.tsx

## Goal
Replace the existing social icons (MessageCircle and Mail) in the footer with proper Telegram and X (Twitter) logos.

## Current State
- File: `src/sections/FinalCTA.tsx`
- Line 4: imports `Zap, Mail, MessageCircle` from `lucide-react`
- Lines 146-163: Two social icon buttons - one using `MessageCircle` (labeled Telegram) and one using `Mail` (labeled Email)

## Implementation Steps

### 1. Create inline SVG components for Telegram and X logos
Since lucide-react doesn't have native Telegram or X icons, create two simple inline SVG React components:
- `TelegramIcon` - SVG of the Telegram paper plane logo
- `XIcon` - SVG of the X (formerly Twitter) logo

### 2. Update imports
- Remove `Mail` and `MessageCircle` from lucide-react imports
- Keep `Zap` (used elsewhere in the file)

### 3. Replace social icon buttons
- Replace the `MessageCircle` icon button with `TelegramIcon`, link to `https://t.me/sol_proxy`
- Replace the `Mail` icon button with `XIcon`, link to the X/Twitter profile (URL TBD - currently uses `#`)

### 4. Update aria-labels
- Update aria-labels to "Telegram" and "X (Twitter)" respectively

## Tradeoffs
- **Inline SVGs vs react-icons**: Inline SVGs avoid adding a new dependency. react-icons would add ~50KB+ to bundle. Inline is cleaner for just 2 icons.
- **X link URL**: The current Mail button links to `#`. The X button will initially use `#` unless a specific X/Twitter URL is provided.
